# TODO — Audio Analytics

Актуальный статус: см. `CURRENT_STATUS.md`. Правила и **главный критерий качества (GT)** — `AGENTS.md`.
Формат: `[ ]` активная, `[x] DONE` — выполнена (оставляем для истории).

> Приоритет задаёт главный критерий: **GT = источник оценки качества; особенно recall реальных покупок**.
> Количество диалогов / coverage / «разумный отчёт» — НЕ метрики качества (см. `AGENTS.md`).

## P0 (критичные — качество клиентских миссий с покупкой)

Приоритет внутри P0:
1. **recall реальных покупок** (каждая GT-продажа найдена как клиентская миссия);
2. **корректное выделение миссии, в которой была покупка** (не сдвинута/не обрублена);
3. **отсутствие ложного объединения** нескольких покупателей в один диалог;
4. **правильное определение `is_sale`**.

- [ ] **Построить корректное сопоставление GT-продаж с клиентскими миссиями (08-27).**
  Подход (без `client_id`): `store_id` + время продажи + содержание транскрипции →
  поиск клиентской миссии → проверка сегментации → проверка `is_sale`.
  Сопоставление **не** по `client_id` (это не источник истины).
- [ ] **Поднять recall реальных покупок при устойчивом precision** (не превращая все
  упоминания покупки в buy). Baseline 08-27: strict recall **0.325**, 39/80 NOT_FOUND,
  15 false-positive buy. NOT_FOUND — это в первую очередь **сегментация** (покупка не
  выделена как миссия): см. соседнюю задачу P0.
- [ ] **Не потерять реальную покупку внутри длинного customer dialog.**
  Симптом: отдельные диалоги 100–200+ минут, внутри — несколько клиентов/покупок/посещений
  (на 08-28/09-03 остаются >30 мин; max 09-03 ≈ 41 мин).
  **Нельзя** hard-cut по времени (например «разрезать по 60 минут») — время это **сигнал на
  LLM-review**, а не порог. Долгий сегмент НЕ считается автоматически одной миссией.
- [ ] **Корректные границы клиентской миссии: не объединять, не резать без причины.**
  При LLM-review использовать признаки:
  - завершение покупки (оплата/прощание) и начало новой встречи;
  - новое приветствие («здравствуйте», «добрый день»);
  - смена темы/товара, смена участника / `seller_id` (из raw-строк);
  - длинные паузы без речи;
  - семантическая связность (единая цель миссии).
  Измеримо: каждая GT-продажа 08-27 → ровно одна клиентская миссия; нет двух покупок в одной.

## P1 (следующие — структура и стабильность качества)

- [ ] **Корректные границы + `dialog_type`/`mission`** (не путать service/help/corporate с buy),
  **отсутствие лишних customer dialogs** (лишние повышают false-positive buy).
- [ ] **GT для проверки качества segmentation**(не только classification): на доступных GT
  оценить не только is_sale, но и «правильно ли выделена миссия / не объединены ли клиенты».
- [ ] **Сегментация: деградация (пустой LLM-ответ → одиночные `unknown`)** — 08-25: 883,
  08-27: 1116, 08-28: 2374. Покрытие строк полное, структура нет. Надёжный retry/fallback,
  диагностика почему LLM молчит на этих чанах (влияет и на recall покупок).
- [ ] **Coverage 2026-09-03: `ok:false` при covered 7969/7969** (+1 `unknown`).
  Разобрать флаг `coverage.ok` (техническая валидность; не заменять качество).
- [ ] **LLM-nondeterminism**: ±1 dialog на одной дате (09-01: 17 vs 18).
  Зафиксировать канонический результат; GT-прогон как стабильный контрольный срез.

## P2 (позже — детали и косметика)

- [ ] Остальные классификационные детали (loss_reason, тонкости role/mission) и
      косметические улучшения отчёта (XLSX/PDF) — не в ущерб GT-качеству (P0/P1).
- [ ] Permissions: файлы `reports/final_transcript_report_2026-09-03*` owned by `root` —
  привести к пользователю `developer` (иначе batch может не перезаписать).
- [ ] Удалить из `COMPARISON.md` устаревшие строки (сравнение OLD vs NEW по 8-25…09-01),
  когда появится новый baseline с P0-разделением. Пока оставить как исторический snapshot.
- [ ] Очистка `_experiment_baseline_08-27.json` и `/reports/документы продаж за 27.08.2026.xlsx`
  — решить: держать как эталон, или перенести в `regression_new/`. Пока — не трогать.
- [ ] Тесты: покрывают `client_id` numbering, PDF, segmentation — расширить на сценарии
  «несколько покупок в одном блоке реплик» (связано с P0).

## DONE (история, не активные)

- [x] **Session 2026-09-04**: полный backfill всех 13 дат через `run.py` (LLM 27b), валидация
  **ALL FINAL VALID + ALL PDF VALID** (coverage 100%, unassigned=0, dup=0, llm_errors=0 по всем),
  бэкап старых finals в `/tmp/opencode/final_backup_20260904_161738/`. Разбор самых длинных
  диалогов **ПОДТВЕРДИЛ** P0-симптом: 08-27 client_3 (3×приветствие) и 08-25 client_11 (2×) —
  несколько клиентов не разделены в одном финальном диалоге. НЕ исправлено (рефакторинг запрещён).
- [x] LLM-сегментация с `client_id` realtime только как debug/нумерации (не источник истины) — в `segmentation.py`.
- [x] PDF-отчёт без session_ids / client_id / raw JSON / CoT; из тех же `final_rows`, без LLM — `pdf_report.py`.
- [x] Backfill всех дат через `batch_run.py` (08-27 и 08-28 в т.ч.), бэкап старых finals в `reports_root_backup/`,
  raw transcripts не тронуты.
- [x] Sales GT эксперимент (`sales_gt.py`, флаг `--sales-ground-truth`) — отдельный режим, по умолчанию off.
- [x] Baseline sales GT 08-27: `_experiment_baseline_08-27.json`.
- [x] Validation-скрипты: `validate_finals.py`, `validate_pdfs.py`, `summary.py`.
- [x] Re-split длинных dialog-сегментов (LLM, включён по умолчанию, `--no-resplit` для отключения) —
  первичный механизм: см. проблему P0 (механизм есть, качество надо поднять).
- [x] Production-конфиг LLM: `qwen3.8:27b` @ `172.18.0.1:11434`, ctx 131072, predict 128000,
  workers 1, retries 2, confidence_min 0.6 — зафиксирован в `CURRENT_STATUS.md`.
- [x] Regression NEW vs OLD — `COMPARISON.md`, `regression_new/` (8 дат, текущий код).
