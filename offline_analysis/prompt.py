"""Prompt-контракты для offline pipeline.

Два независимых этапа (требование №3 — сначала segmentation, потом classification):
    1. SEGMENTATION  — восстановление структуры дня из потока реплик
       (без client_id). Результат: список сегментов с границами строк.
    2. CLASSIFICATION — классификация каждого выделенного диалога
       (role / mission / is_sale / loss_reason / confidence / reason).
"""

from __future__ import annotations

from typing import Sequence

from .taxonomy import (
    LOSS_REASONS,
    MISSIONS,
    MISSION_TO_DIALOG_TYPE,
    SEGMENT_TYPES,
)


# ---------------------------------------------------------------------------
# Этап 1: SEGMENTATION (без client_id)
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Этап 1: SEGMENTATION (без client_id).
#
# 2026-09-11: промт переписан под «holistic» подход (требование: сначала
# восстановление структуры клиентов ГРАНИЦАМИ, потом отдельно классификация).
# Главный вопрос к каждой границе — «продолжение разговора или новый клиент».
# Приветствия/прощания — лишь СЛАБЫЕ признаки, НЕ правило. Сильный акцент на
# минимизацию SPLIT (перерезание одного реального клиента на несколько).
# Количество клиентских диалогов — РЕЗУЛЬТАТ анализа, а не заранее заданная цель.
# ВЯЗКА с кодом: ответ обязан покрывать ВСЕ строки фрагмента (индексы) —
# downstream (repair_segments) строит отчёт из индексов строк.
# ---------------------------------------------------------------------------

SEGMENTATION_SYSTEM_PROMPT = """
# ROLE

Ты выполняешь только сегментацию транскрипции.

Твоя задача — восстановить реальные клиентские диалоги (и отдельно: разговоры
сотрудников, фоновая речь) из последовательности распознанных реплик одного
магазина за рабочий период.

НЕ определяй (это отдельный этап):
- миссию клиента;
- покупку / результат визита;
- качество обслуживания, эмоции, тип клиента.

Классификация «что хотел клиент / была ли покупка» — НЕ твоя задача.
Твоя задача — только ГРАНИЦЫ и ТИП каждого фрагмента.

# INPUT

Ты получаешь последовательность реплик за один рабочий период одного микрофона
магазина. Для каждой реплики доступны:
- индекс строки (номер, первая колонка) — ГЛОБАЛЬНЫЙ 0-based внутри этого магазина;
- временна́я метка (HH:MM:SS);
- текст (ASR содержит ошибки распознания и шум).

speaker / seller_id НЕ являются сигналом: в данных один и тот же seller_id стоит
на всех строках, а session_id — кусок VAD, не клиент. Оперируй ТОЛЬКО текстом
и временем.

# TASK

Раздели ПОЛНЫЙ поток реплик на последовательность фрагментов.

Каждый фрагмент — ОДНО цельное событие:
- dialog     — один реальный визит/контакт ОДНОГО клиента к продавцу;
- employee   — разговор сотрудников между собой (без клиента);
- background — фоновая/посторонняя речь, шум, тесты микрофона, телефонные звонки;
- unknown    — невозможно уверенно определить, что происходит.

Главный вопрос при выборе каждой ГРАНИЦЫ:
> Продолжает ли текущий отрезок уже существующий разговор
>  ИЛИ начинается новое взаимодействие с другим клиентом (другое событие)?

# IMPORTANT (сигналы, которые НЕ являются правилами)

НЕ используй как правило:
  "здравствуйте" = новый клиент
  "до свидания" = конец клиента

Это только дополнительные признаки. Один клиент может:
- не сказать «здравствуйте»;
- не сказать «до свидания»;
- сделать длинную паузу и вернуться;
- продолжить разговор после фразы, похожей на завершение;
- несколько раз поздороваться внутри ОДНОГО разговора;
- сменить тему внутри одного разговора (с цена на гарантию).

# SIGNALS THAT SUPPORT CONTINUATION (один клиент)

Считай, что разговор ПРодолжается, если совокупность признаков показывает
смысловую непрерывность:
- продавец продолжает отвечать на предыдущий вопрос клиента;
- клиент продолжает обсуждать тот же товар/услугу;
- сохраняется одна тема или последовательность шагов одной сделки
  (цена -> выбор -> оплата -> чек -> гарантия у ОДНОГО клиента);
- реплика естественным образом продолжает предыдущую;
- временной промежуток небольшой;
- нет убедительного признака появления другого нового клиента.

# SIGNALS THAT SUPPORT A NEW CLIENT (новый визит)

Новое взаимодействие более вероятно, если совокупность признаков показывает:
- завершение предыдущего разговора (прощание/«будем рады видеть снова»/чек/оплата)
  + появление новОГО обращения по тем е, кто не упоминался раньше;
- появление нового человека, НЕ участвовавшего в предыдущем разговоре;
- между блоками — другой завершённый разговор (сотрудников или другого клиента);
- явно новая сделка: новые товар/модель/автомобиль/сумма, обсуждение которых
  было НЕ в предыдущем разговоре.

# AMBIGUOUS CASES

Не создавай нового клиента/фрагмента только потому, что существует один слабый
признак. Если сомневаешься между
  A) продолжением существующего разговора  И  B) новым клиентом,
анализируй контекст ДО и ПОСЛЕ текущей реплики и предпочитай наиболее связное
объяснение всей последовательности.

# MAIN PRINCIPLE — найти ГРАНИЦЫ между реальными клиентами; баланс ошибок

1. GREATEST DANGER (MERGE) — несколько РАЗНЫХ клиентов, пришедших подряд
   (один купил/убрался -> новый пришёл и задал вопрос), объединяются в ОДИН
   длинный «диалог». Это самая частая ошибка. Каждый отдельный клиентский
   визит — СВОЙ фрагмент, даже если между ними мало времени (30 секунд!) и
   даже если они обсуждают похожие товары. Если ты видишь внутри блока
   «покупка A завершилась (чек/оплата/прощание) + новое обращение нового
   человека (здоров/новый товар/новый автомобиль)», это ОБЯЗАТЕЛЬНО ДВА
   фрагмента с границей между ними. НЕ объединяй таких клиентов в один.
2. SPLIT — один реальный клиент разбит на несколько. Не разделяй ОДИН
   непрерывный разговор (один человек, одна сделка: цена -> выбор -> оплата ->
   чек, даже если он длится 30+ минут) на части только из-за короткой паузы,
   отдельного приветствия или смены подтемы (цена -> гарантия) ВНУТРИ одной
   сделки.

Иными словами: ЧАЩЕ всего нужно РАЗДЕЛЯТЬ разных клиентов (MERGE — главная
опасность); реже — не РАЗБИВАТЬ одного клиента (SPLIT). Определяющий критерий
границы: «завершилось ли предыдущее взаимодействие + началась ли новая
инициатива другого человека»?

# КОРОТКИЕ И ШУМНЫЕ Клиентские Визиты (ВАЖНО)

Короткий визит (1-3-реплики: «сколько стоит» → «беру», «спасибо», «до свидания»,
«у меня вот вопрос про АКБ» → «да, есть») — это ПОЛНЫЙ клиентский диалог, и он
ОБЯЗАТЕЛЕН в сегментации. Не скрывай его внутри «employee»/«background»-сегмента
на том основании, что:
  * реплики клиента выглядят обрывочно/коротко;
  * рядом (в нескольких минутах) идёт разговор сотрудников;
  * ASR распознал клиентские слова как шум.

Если в интервале между двумя employee-сегментами есть 1+ реплика, которая
звучит как обращение к продавцу / ответ продавцу по клиентскому вопросу
(цена, наличие, модель, покупка, гарантия, «сколько стоит/есть/есть такой») —
выдели этот интервал как dialog, НЕ поглощай его в employee/background.

Наоборот: НЕ считай автоматом «employee» весь блок, где есть смешение. Внутри
одного «employee»-интервала может быть вкраплен короткий клиентский визит
(клиент заговорил в середине разговора сотрудников, или два визита подряд без
явного приветствия) — его нужно выделить ОТДЕЛЬНО.

Разговоры сотрудников между двумя клиентскими визитами — отдельный employee-
фрагмент (ИМЕННО он может увеличивать общее число сегментов — это нормально).
Посторонняя речь/шум/тест микрофона — background, НЕ клиентский разговор.

# CRITICAL RESTRICTION

- Не предугадивай количество клиентов заранее; количество — результат анализа.
- Не используй human annotation и НЕ подгоняй границы под какое-то число.
- Опирайся только на доступный текст и время.

# OUTPUT

Верни СТРОГО ОДИН JSON-объект (без markdown, без пояснений вокруг него).
Формат:
  {"segments": [
     {"start_row": <int>, "end_row": <int>,
      "type": "<dialog|employee|background|unknown>",
      "confidence": <число 0..1>,
      "reason": "<короткая причина границы/типа, на русском, <= 200 символов>"}
  ]}

start_row / end_row — ГЛОБАЛЬНЫЕ 0-based индексы строк (из первой колонки),
end_row инклюзив. Соседние фрагменты без пересечений:
  segments[i+1].start_row == segments[i].end_row + 1.
Первый фрагмент ОБЯЗАТЕЛЬНО start_row = минимальный индекс,
последний — end_row = максимальный индекс.
Совокупность фрагментов ПОКРЫВАЕТ ВСЕ строки от первой до последней,
без дублей и без пропусков.

Качество и правильность ГРАНИЦ ВАЖНЕЕ количества сегментов.
"""


# ---------------------------------------------------------------------------
# Этап 1b: RE-SEGMENTATION (доп. проверка слишком длинного dialog-сегмента)
# ---------------------------------------------------------------------------
# Применяется ТОЛЬКО к тем dialog-сегментам, которые первичная сегментация
# поместила в один блок на подозрительно большую длительность (>= review/very
# long порог). Цель — найти внутри ВНУТРЕННИЕ границы независимых клиентских
# взаимодействий (покупка A -> завершение -> клиент B -> ...), НО не резать
# один действительно продолжительный разговор (поэтому model может ответить
# segments:[] = «оставить как есть»).
#
# ВАЖНО: ответ должен покрывать РОВНО тот интервал строк [start..end], т.е.
# либо [ [start..end] ] (1 сегмент — разговор единый), либо набор соседних
# сегментов, которые вместе ПОКРЫВАЮТ весь [start..end] без дублей и пропусков.
# Это обеспечивает инвариант coverage.

RESEGMENT_LONG_SYSTEM_PROMPT = (
    "Ты — аналитик аудиопотока магазина (автосервис / магазин аккумуляторных батарей).\n"
    "РАЗБИВАЙ ТОЛЬКО по смыслу. НЕТ жёсткого временного порога.\n\n"
    "ТВОЯ ЗАДАЧА: перед тобой ОДИН длинный фрагмент, который первичная сегментация "
    "объединила в ОДИН «диалог». Такие длинные фрагменты ЧАСТО содержат несколько "
    "последовательных клиентских взаимодействий (клиент A покупка -> чек/гарантия -> "
    "пауза/звонки/другие события -> клиент B покупка -> ...), которые НЕ должны быть "
    "объединены в один «диалог».\n\n"
    "Раздели фрагмент на НЕПЕРЕКРЫВАЮЩИЕСЯ блоки по времени. Каждый блок — ОДНО "
    "логически связное взаимодействие с ОДНИМ клиентом / одной группой клиентов "
    "вокруг одной покупки. Найдя границу, укажи её как конец одного блока и "
    "начало следующего.\n\n"
    "Критерии НОВЫХ границ (ищи ВСЕ, но используй как СОВОКУПНУЮ картину, не по одному признаку):\n"
    "  1. Завершение предыдущего разговора: «до свидания», «спасибо», «будем рады видеть снова», "
    "выдача чека/оплаты/гарантийного талона, списание бонусов, «всё готово».\n"
    "  2. Появление нового приветствия: «здравствуйте», «добрый день», «да, здравствуйте», "
    "«ага, и?».\n"
    "  3. Смена объекта/темы: новая модель АКБ, новый автомобиль, новый вопрос типа "
    "«а сколько такой-то стоит». Если в конце блока A говорят «до свидания», а в начале "
    "блока B — «сколько стоит такой-то» — это 2 РАЗНЫХ клиента.\n"
    "  4. Завершённая покупка: оплата картой/наличкой + выдача чека/гарантийного талона "
    "+ «пройдём/будём ждать вас» — это СВЕРХ-СИЛЬНАЯ граница. Если после неё "
    "начинается новый самостоятельный разговор — это новый блок.\n"
    "  5. Длительная пауза (5+ минут) между блоками — признак границы, НО не единственный "
    "(бывают паузы без смены клиента, и бывает смена клиента без паузы).\n"
    "  6. Несколько независимых покупок с разными суммами/моделями/клиентами.\n"
    "  7. Повторное «сколько стоит/есть/есть такой-то» после завершения предыдущего "
    "обращения.\n\n"
    "ВАЖНО — НЕ делай этого (ложные срабатывания):\n"
    "  • Не режь длинный, НО ОДИН непрерывный разговор только по времени. Если клиент "
    "1,5 часа выбирает АКБ, спорит о цене, торгуется, получает бонусы — это ОДИН "
    "взаимодействие, и ответ должен быть segments:[] (оставить единым).\n"
    "  • Не считай каждое упоминание слова «купил» границей. Нужно РЕШЕНИЕ по "
    "контексту: была ли ЗАВЕРШЕННАЯ покупка (чек+оплата+прощание) или "
    "просто обсуждение.\n"
    "  • Не объединяй в один блок два РАЗНЫХ клиента, даже если между ними пауза "
    "в 30 секунд. Два приветствия подряд = два разных клиента. Две завершённые "
    "покупки подряд = два разных блока.\n"
    "  • Не дробь фон/шум/разговор сотрудников внутри блока — если внутри "
    "длинного фрагмента есть employee-сегмент, его можно выделить отдельно "
    "(type=employee), но ОСТАЛЬНЫЕ клиентские блоки — по клиентам.\n\n"
    "ОТВЕТ — строго ОДИН JSON-объект (без markdown, без комментариев), поля:\n"
    '  {"segments": [\n'
    '     {"start_row": <int, 0-based, ГЛОБАЛЬНЫЙ индекс>, "end_row": <int, ГЛОБАЛЬНЫЙ, инклюзив>, '
    '"type": "<dialog|employee>", "confidence": <0..1>, '
    '"reason": "<коротко, <= 200 символов, на русском>"}\n'
    "  ]}\n\n"
    "ПРАВИЛА:\n"
    "  • Первый сегмент — ОБЯЗАТЕЛЬНО начинается с первой строки фрагмента: "
    "start_row = <первая строка>.\n"
    "  • Последний сегмент — ОБЯЗАТЕЛЬНО заканчивается на последней строке фрагмента: "
    "end_row = <последняя строка>.\n"
    "  • Соседние сегменты — без дублей и без пропусков. Сумма строк = длина фрагмента.\n"
    "  • Допустимые type: 'dialog' (клиентский) или 'employee' (сотрудники между собой). "
    "background/unknown — НЕ использовать: внутри длинного client-fragment это "
    "почти всегда разговор клиента или сотрудников.\n"
    "  • Если это ОДНО действительно непрерывное взаимодействие (один клиент, "
    "один процесс покупки), верни segments:[] (пустой список) — значит, "
    "оставить сегмент как есть.\n"
    "  • quality > quantity: лучше оставить единым, чем порезать по времени.\n"
)


def build_long_dialog_resegment_prompt(
    store_id: str,
    rows: Sequence,
    is_very_long: bool = False,
    greet_count: int | None = None,
    end_count: int | None = None,
    forced: bool = False,
    greet_positions: "Sequence[int] | None" = None,
    end_positions: "Sequence[int] | None" = None,
) -> str:
    """Пользовательский prompt для дополнительного разделения длинного dialog-сегмента.

    ``rows`` — именно тот блок строк, который первичная сегментация оставила
    единым. Индексы ГЛОБАЛЬНЫЕ (Row.index), т.к. Segment.start_index/end_index
    тоже глобальные.

    ``greet_count`` / ``end_count`` — детерминированный счётчик приветствий
    и маркеров завершения (чек/оплата/прощание) в блоке. Выдаётся LLM-модели
    как ФАКТ, чтобы она не «забыла» маркеры (см. checkpoint §3.2). Для одного
    действительно длинного разговора счётчик ``greet_count == 1`` — LLM
    честно вернёт segments:[]. Для 2+ независимых приветствий счётчик
    ``greet_count >= 2`` — ЛЛМ ОБЯЗАНА начать искать границы.

    ``forced`` — сегмент попал на ре-сплит НЕ по длительности, а по
    force-trigger (например, ≥2 приветствия в блоке < 30 мин).

    ``greet_positions`` / ``end_positions`` — ГЛОБАЛЬНЫЕ индексы строк, где
    встретился каждый маркер (2026-09-03 iter.1). Конкретные индексы вместо
    абстрактных счётчиков — LLM-модели (qwen3.8:27b) легче принять решение,
    когда у неё под рукой конкретные строки для границы.
    """
    lines = []
    for r in rows:
        text = " ".join((r.text or "").split())
        lines.append(f"{r.index} | {r.created_at:%H:%M:%S} | {text}")
    body = "\n".join(lines)
    first = rows[0].index
    last = rows[-1].index
    duration_text = _fmt_duration(rows)
    emphasis = (
        "ЭТО ОЧЕНЬ ДЛИННЫЙ фрагмент — практически всегда внутри него несколько "
        "независимых клиентских взаимодействий. Оставь единым только тогда, "
        "когда это ОЧЕВИДНО ОДИН неразрывный разговор с одним клиентом.\n\n"
        if is_very_long else
        "ЭТО ДЛИННЫЙ фрагмент — может содержать один разговор, а может несколько "
        "последовательных клиентских взаимодействий. Разбей на блоки, если "
        "видишь явные границы (завершение+новое начало). Если это ОДНА "
        "непрерывная покупка с одним клиентом — оставь как есть (segments:[]).\n\n"
    )

    # --- Счётчик маркеров как ФАКТ (checkpoint §3.2) ---
    marks_facts: list[str] = []
    if greet_count is not None:
        marks_facts.append(
            f"  приветствий (здравствуйте/добрый день/добрый вечер): {greet_count}"
        )
    if end_count is not None:
        marks_facts.append(
            f"  завершений (чек/оплата/прощание/спасибо/бонусы): {end_count}"
        )
    # 2026-09-03 iter.1: конкретные индексы вместо только счётчика.
    pos_block = _positions_block(rows, greet_positions, end_positions)
    if pos_block:
        marks_facts.append("")
        marks_facts.append(pos_block.rstrip())
    if marks_facts:
        marks_block = (
            "FAKT (детерминированный счёт в блоке, выдан как данные, не как текст):\n"
            + "\n".join(marks_facts)
            + "\n"
        )
        if greet_count is not None and greet_count >= 2:
            marks_block += (
                f"  => {greet_count} независимых приветствий внутри ОДНОГО блока — "
                "это ОБЯЗАТЕЛЬНО 2+ взаимодействий клиентов. "
                "Каждое (кроме первого) приветствие, которое предшествует "
                "завершение предыдущего взаимодействия (чек/оплата/прощание), "
                "— ГРАНИЦА между блоками. "
                "Не верни segments:[]. "
                "Верни 2+ сегмента: [0..end_prev_block] и [start_new..last] "
                "для каждого последующего приветствия.\n"
            )
    else:
        marks_block = ""

    forced_note = (
        "ВНИМАНИЕ: этот фрагмент попал на ре-сплит НЕ по длительности, а по "
        "детерминированному маркеру (2+ приветствия внутри). Оставь единым "
        "segments:[] только если это РЕАЛЬНО один клиент, повторившийся "
        "внутрь разговора. Если приветствия относятся к разным клиентам — "
        "разбей на блоки.\n\n"
        if forced else ""
    )

    return (
        f"КОНТЕКСТ:\n"
        f"  store_id: {store_id}\n"
        f"  fragment_rows: {first}..{last} (всего {len(rows)} строк)\n"
        f"  fragment_duration: {duration_text}\n\n"
        f"{marks_block}"
        f"{forced_note}"
        f"Поток реплик (формат: index | HH:MM:SS | текст, ASR содержит ошибки и шум):\n"
        f"{body}\n\n"
        f"{emphasis}"
        f"Первичная сегментация объединила это в ОДИН dialog-сегмент. Раздели "
        f"на НЕПЕРЕКРЫВАЮЩИЕСЯ блоки по логическим границам (завершение одного "
        f"взаимодействия + новое начало другого) или верни segments:[] "
        f"если это ОДНО непрерывное взаимодействие с одним клиентом. "
        f"Ответь одним JSON-объектом."
    )


def _positions_block(
    rows: Sequence,
    greet_positions: "Sequence[int] | None",
    end_positions: "Sequence[int] | None",
) -> str:
    """Форматирует конкретные индексы/время маркеров (2026-09-03 iter.1).

    LLM-модели проще принять решение при конкретных строках/время вместо
    только численного счётчика (обучено: qwen3.8:27b стабильно ignores
    «greet_count=2» и возвращает segments:[]; с конкретными индексами
    LLM видит, что есть конкретный набор boundary-кандидатов).
    """
    out: list[str] = []
    index_to_time = {r.index: r.created_at for r in rows}

    def _fmt(pos: int) -> str:
        t = index_to_time.get(pos)
        return f"{pos} ({t:%H:%M:%S})" if t else f"{pos}"

    if greet_positions:
        out.append(
            f"  приветствия (позиции): {', '.join(_fmt(p) for p in greet_positions)}"
        )
    if end_positions:
        out.append(
            f"  завершения (позиции): {', '.join(_fmt(p) for p in end_positions)}"
        )
    return "\n".join(out) if out else ""


def _fmt_duration(rows: Sequence) -> str:
    """Человекочитаемая длительность фрагмента (минуты/часы)."""
    if not rows:
        return "0 мин"
    first = rows[0].created_at
    last = rows[-1].created_at
    delta = last - first
    total_min = delta.total_seconds() / 60.0
    hours = int(total_min // 60)
    mins = int(total_min % 60)
    if hours:
        return f"{hours}ч {mins:02d}мин"
    if total_min >= 1:
        return f"{total_min:.1f} мин"
    return f"{int(total_min*60)} сек"


# ---------------------------------------------------------------------------
# Этап 1c: RESCAN non-dialog blocks (iter_03 hypothesis).
#
# Основная сегментация видит ОДИН большой чанк (~30k токенов) целиком, поэтому
# короткий клиентский визит внутри длинного employee/background-блока
# «поглощается» и получает type=employee/background. Второй проход видит ТОЛЬКО
# этот (гораздо меньший) блок и обязан решить: есть ли внутри РЕАЛЬНЫЙ
# клиентский диалог; если нет — оставить единым (segments:[]).
#
# Контракт: segments покрывают РОВНО [first..last] глобальных индексов
# (как RESEGMENT_LONG) — downstream `_parse_resegment_response` +
# `repair_segments` уже дают coverage-инвариант. Допустимые type:
#   dialog, employee, background, unknown
# (внутри non-dialog блока background/unknown — легитимные, ОСТАВЛЯЕМ).
# ---------------------------------------------------------------------------

RESCAN_NONDIALOG_SYSTEM_PROMPT = (
    "Ты — аналитик аудиопотока магазина. Перед тобой ОДИН ФРАГМЕНТ, который "
    "первичная сегментация пометила КАК employee/background/unknown (т.е. "
    "«не клиентский»). Твоя задача — ПРОВЕРИТЬ, не спрятан ли внутри этого "
    "фрагмента РЕАЛЬНЫЙ клиентский диалог, который первичный проход «загнал» "
    "в employee/background.\n\n"
    "ПРИЗНАКИ клиентского диалога ВНУТРИ non-dialog блока:\n"
    "  • прямое обращение клиента к продавцу: «сколько стоит», «есть такой-то», "
    "«а что с АКБ», «мне нужен…», «сколько по гарантии»;\n"
    "  • ответ продавца клиенту по конкретному товару/цены/модели;\n"
    "  • завершённая покупка: «беру», «оформляем», «пройдемте на кассу», "
    "«терминал», «чек», «оплата»;\n"
    "  • даже 1-3 короткие реплики (клиент зашёл, спросил, ушёл) = ОДИН "
    "client-диалог.\n\n"
    "ЧТО НЕ ЯВЛЯЕТСЯ клиентским (оставляем employee/background/unknown):\n"
    "  • разговор сотрудников между собой (оборудование, смена, документы, "
    "тесты микрофона, бытовуха);\n"
    "  • фоновая речь, тесты, шум;\n"
    "  • разрозненные обрывки без явного обращения к продавцу.\n\n"
    "ВАЖНО:\n"
    "  • НЕ объединяй и НЕ дробь: найди ТОЛЬКО те интервалы, которые реально "
    "являются клиентским обращением. Остальное — оставь как есть (тот же "
    "type, что была в блоке, или employee/background/unknown по смыслу).\n"
    "  • Если ВНУТРИ блока видно «сотрудники говорят -> клиент зашёл -> "
    "сотрудники говорят -> второй клиент зашёл» — это 2 ОТДЕЛЬНЫХ "
    "dialog-фрагмента, между ними — employee/background.\n"
    "  • НЕ выдумывай: если клиентского обращения нет — верни segments:[] "
    "(блок остаётся единым, без изменений).\n"
    "  • Границы — ПО СМЫСЛУ (начало обращения -> конец), не по времени.\n\n"
    "ОТВЕТ — строго ОДИН JSON-объект (без markdown, без комментариев), поля:\n"
    '  {"segments": [\n'
    '     {"start_row": <int, ГЛОБАЛЬНЫЙ индекс>, "end_row": <int, ГЛОБАЛЬНЫЙ, инклюзив>, '
    '"type": "<dialog|employee|background|unknown>", "confidence": <0..1>, '
    '"reason": "<коротко, <= 200 символа, по-русски>"}\n'
    "  ]}\n\n"
    "ПРАВИЛА:\n"
    "  • Первый сегмент — start_row = <первая строка блока>.\n"
    "  • Последний сегмент — end_row = <последняя строка блока>.\n"
    "  • Соседние сегменты — без дублей и без пропусков (coverage [первая..последняя]).\n"
    "  • Допустимые type: 'dialog', 'employee', 'background', 'unknown'.\n"
    "  • Если ОДНОГО клиентского обращения ВНУТРИ блока нет — верни "
    "segments:[] (пустой массив) — блок остаётся единым, без изменений.\n"
)


def build_rescan_nondialog_prompt(
    store_id: str,
    rows: Sequence,
    original_type: str,
) -> str:
    """User prompt для second-pass rescans non-dialog блока (iter_03).

    ``rows`` — строки блока, который первичная сегментация пометила как
    employee/background/unknown. ``original_type`` — исходный type (для
    контекста). Индексы ГЛОБАЛЬНЫЕ (Row.index).
    """
    lines = []
    for r in rows:
        text = " ".join((r.text or "").split())
        lines.append(f"{r.index} | {r.created_at:%H:%M:%S} | {text}")
    body = "\n".join(lines)
    first = rows[0].index
    last = rows[-1].index
    duration_text = _fmt_duration(rows)
    return (
        f"КОНТЕКСТ:\n"
        f"  store_id: {store_id}\n"
        f"  block_rows: {first}..{last} (всего {len(rows)} строк)\n"
        f"  block_duration: {duration_text}\n"
        f"  primary_label: {original_type}\n\n"
        f"Поток реплик (формат: index | HH:MM:SS | текст, ASR содержит ошибки и шум):\n"
        f"{body}\n\n"
        "Первичная сегментация пометила этот блок КАК non-dialog. "
        "Проверь: есть ли ВНУТРИ блока реальный клиентский диалог (обращение "
        "клиента к продавцу / покупка)? Ответь одним JSON-объектом (см. CONTRACT "
        "в system prompt). Если клиентского обращения нет — segments:[] "
        "(оставить как есть)."
    )


def build_segmentation_user_prompt(
    store_id: str,
    rows: Sequence,
    chunk_index: int = 1,
    chunk_count: int = 1,
    sales_snippet: str | None = None,
) -> str:
    lines = []
    for r in rows:
        text = " ".join((r.text or "").split())
        lines.append(f"{r.index} | {r.created_at:%H:%M:%S} | {text}")
    body = "\n".join(lines)
    total = len(rows)
    span = f"Строк в этом фрагменте: {total}\n" if total <= 1 else ""
    tail = (
        (sales_snippet or "").rstrip()
    )
    return (
        f"КОНТЕКСТ:\n"
        f"  store_id: {store_id}\n"
        f"  chunk: {chunk_index}/{chunk_count}\n"
        f"  {span}"
        "  Индексы строк (start_row / end_row) — ГЛОБАЛЬНЫЕ 0-based, берутся из первой колонки ниже.\n\n"
        f"{tail}"
        "Поток реплик (формат: index | HH:MM:SS | текст, ASR содержит ошибки и шум):\n"
        f"{body}\n\n"
        "Сегментируй ЭТОТ фрагмент: разбей на диалоги/события. "
        "Если фрагмент начинается или заканчивается посредине разговора — так и отмечай "
        "(это стык с соседним фрагментом, не выдумывай недостающее). "
        f"{_sales_boundary_hint(sales_snippet)}"
        "Ответь одним JSON-объектом."
    )


def _sales_boundary_hint(sales_snippet: str | None) -> str:
    if not sales_snippet:
        return ""
    return (
        "ИСПОЛЬЗУЙ времена реальных продаж как ДОПОЛНИТЕЛЬНЫЕ ориентиры для границ "
        "клиентских диалогов (покупка A и покупка B — это разные dialog-сегменты). "
        "НО НЕ делай правила «каждая продажа = отдельный сегмент» на слепую: "
        "опирайся на текст.\n\n"
    )



# ---------------------------------------------------------------------------
# Этап 2: CLASSIFICATION (на готовый dialog-сегмент)
# ---------------------------------------------------------------------------

SYSTEM_PROMPT = (
    "Ты — аналитик клиентского диалога автосервиса (магазин по продаже и обслуживанию аккумуляторных батарей).\n"
    "Тебе даёт ГРАНИЦЫ И ТЕКСТ ОДНОГО выделенного разговора (ASR, возможны ошибки распознавания и фоновый шум). "
    "Твоя задача — определить, КТО говорит и о ЧЁМ речь, классифицировать обращение клиента, был ли факт покупки "
    "и (для миссии «Купить») почему покупка не состоялась.\n\n"
    "Возможные роли (role):\n"
    "  customer   — реальное обращение клиента к продавцу / разговор продавца с клиентом;\n"
    "  employee   — разговор сотрудников между собой (без участия клиента);\n"
    "  background — фоновые/случайные/не относящиеся к клиентскому обслуживанию реплики;\n"
    "  unknown    — невозможно уверенно определить, кто говорит и что происходит.\n\n"
    "Допустимые миссии (mission) — строго одна из списка выше, либо \"unknown\":\n"
    + "\n".join("  - " + m for m in MISSIONS) + """

Если речь не о клиенте (role != customer) — mission = "unknown", is_sale = false, loss_reason = null.

is_sale — true ТОЛЬКО когда по контексту ЯВНО следует, что покупка состоялась (выбор товара + подтверждение / оплата /
"беру", "оформляем", "пройдемте на кассу", "принесите деньги"). Наличие слов "купить", "цена", "сколько стоит",
"интересует" НЕ является доказательством покупки. Для миссий, не связанных с покупкой батарей, is_sale = false.

loss_reason — заполняется ТОЛЬКО когда role=customer AND mission="Купить" AND is_sale=false.
Допустимые значения (строго одно из), либо null:
"""
    + "\n".join("  - " + r for r in LOSS_REASONS)
    + """

Если ни одна из допустимых причин не подходит уверенно — loss_reason = null.

confidence — твоя уверенность в определении role + mission (0..1).
Если текста мало, очень шумный, неоднозначный — confidence ниже 0.6.

reason — КРАТКОЕ ВНЕШНЕЕ объяснение решения (<= 250 символов, на русском).
НЕ пиши цепочку рассуждений / скрытый ход мыслей — только итоговое объяснение по фактам.

ВЕРНИ СТРОГО ОДИН JSON-объект (без markdown, без комментариев):
{"role":"<customer|employee|background|unknown>","mission":"<одна из миссий или unknown>","is_sale":<true|false>,"loss_reason":<одна из допустимых причин или null>,"confidence":<число 0..1>,"reason":"<краткое внешнее объяснение решения по фактам, <= 250 символов>"}
"""
)


def build_classification_user_prompt(
    store_id: str,
    seller_id: str | None,
    segment_text_with_times: str,
    sales_snippet: str | None = None,
) -> str:
    sales_block = (sales_snippet or "").rstrip()
    if sales_block:
        sales_block = (
            "\n\nКОНТЕКСТ О РЕАЛЬНЫХ ПРОДАЖАХ (документ ККМ):\n"
            f"{sales_block}\n"
            "Если этот диалог по времени и тексту соответствует одной из реальных "
            "продаж — это завершённая покупка: mission=«Купить», is_sale=true.\n"
            "Если диалог НЕ соответствует ни одной реальной продаже — НЕ выдавай "
            "is_sale=true по одной лишь похожести (это будет ложноположительная покупка).\n"
        )
    else:
        sales_block = "\n"
    return (
        "КОНТЕКСТ:\n"
        f"  store_id: {store_id}\n"
        f"  seller_id: {seller_id if seller_id else '(не определён)'}\n"
        f"{sales_block}\n"
        "ТЕКСТ ОДНОГО РАЗГОВОРА (время | текст, ASR содержит ошибки и шум):\n"
        f"{segment_text_with_times}\n\n"
        "Ответь одним JSON-объектом."
    )


def format_segment_with_times(segment) -> str:
    """Форматирует rows сегмента для классификации: ``HH:MM:SS | text`` на строку."""
    lines = []
    for r in segment.rows:
        ts = r.created_at.strftime("%H:%M:%S")
        text = " ".join((r.text or "").split())
        lines.append(f"{ts} | {text}")
    return "\n".join(lines)


# Сохраняем старое имя для обратной совместимости (legacy).
def format_block_with_times(block) -> str:
    return format_segment_with_times(block)
